# Drop the CA certificate downloaded from your database provider here.
# Aiven console -> your MySQL service -> Connection information -> CA certificate.
# Then set MYSQL_SSL_CA=certs/aiven-ca.pem and MYSQL_SSL_MODE=VERIFY_IDENTITY in .env.
